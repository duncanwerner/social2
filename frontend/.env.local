# Local frontend dev override (Vite reads .env.local; git-ignored).
# Point the dev server at the deployed Worker instead of the default
# http://localhost:8787. Delete this file to go back to a local backend.
VITE_BACKEND_URL=https://do-sockets-backend.trebdev.workers.dev
